package com.project.accountDisplay.service;

/**
 * Class which reads clients transactions from internet database file and returns for its consumers
 */

public class AccountReader {
	
	/*
	 * // list to hold the retrieved transactions private List transactions = new
	 * ArrayList(); private Iterator transactionIterator = null; public void
	 * loadData(String url) { try { // create new URL object URL webpageUrl = new
	 * URL(url); // create reader to get stream BufferedReader in = new
	 * BufferedReader(new InputStreamReader(webpageUrl.openStream())); String
	 * inputLine = null; // retrieve through the file content and store it in the
	 * list while ((inputLine = in.readLine()) != null) { String strSplit[] =
	 * inputLine.split(","); transactions.add(strSplit); } // close the stream
	 * in.close(); } catch(Exception e) { System.out.println("Exception e"+
	 * e.getMessage()); } // iterator to hold the retrieved list values
	 * transactionIterator = transactions.iterator(); } public int
	 * getNumberTransactions() { return transactions.size(); } public String[]
	 * getNext() { return (String[]) transactionIterator.next(); }
	 */
}
